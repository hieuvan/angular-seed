'use strict';

define(function() {
  return [function() {
    return {
      save: 'Form is saved successfully',
      saveError: 'Form could not be saved',
      newFolder: 'New folder is added',
      addItem: 'Item added to the tree',
      removeItem: 'Item removed from the tree'
    };
  }];
});
